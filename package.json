{
  "name": "bootstrap",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm ci && composer install && npm run build",
    "sync-theme": "node sync-theme-json.js",
    "dev": "rm -rf build && npm run sync-theme && wp-scripts start",
    "build": "rm -rf build && npm run sync-theme && wp-scripts build",
    "lint:js": "wp-scripts lint-js",
    "lint:php": "composer run phpcs",
    "lint": "npm run lint:js && npm run lint:php",
    "phpstan": "composer run phpstan",
    "format:js": "wp-scripts format",
    "format:php": "composer run phpcbf",
    "format": "npm run format:js && npm run format:php"
  },
  "author": "Dhaval Parekh",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bootstrap": "^5.3.7"
  },
  "devDependencies": {
    "@wordpress/scripts": "^31.8.0"
  }
}
